pandas>=2.2
numpy>=1.26
yfinance>=0.2.40
pyyaml>=6.0
pytest>=8.0
statsmodels>=0.14
quantstats>=0.0.62
# not required for sma_crossover_sp500top50 specifically, but part of the
# project's approved stack per CLAUDE.md §4 / quant-researcher-methodology.md
# -- install as later strategies need them:
# scikit-learn
# pandas-ta
# PyPortfolioOpt
# Riskfolio-Lib     # risk-parity / commodities cross-asset strategy specifically -- don't install alongside PyPortfolioOpt for the same strategy
# arch
# alpaca-py
# alphalens-reloaded  # cross-sectional momentum / factor-ranking strategy specifically
# ccxt                # crypto strategy only, once scoped
# vectorbt / backtrader  # one-off sweep tool only -- see CLAUDE.md §4 maintenance caveats, never the default engine
